/// GI 路径的旋钮（光路求值 / ReSTIR 复用 / 降噪共用）。
/// Rust 需要的那几个经 `wesl_consts.rs` 的 `REQUIRED` 解析（只认 `u32` 字面量）。

/// 探索射线 / 阴影射线的 t 上限（voxel）。
/// 不要靠调小它来省帧时：`trace.wesl` 的 `trace_grid` 已用世界 AABB slab 把遍历夹到
/// `min(bx_exit, 本值)`，所以真正的杠杆是 `trace.wesl::SECONDARY_REACH_CHUNKS`（那个按 chunk 夹，
/// 实测 12 → 1 能让 `gate_gi` 16.2 → 6.2 ms）。本值只决定"多远的命中被当成天光"，
/// 调小会让 >本值的命中变成天光（像素值变化），而代价通常不变。
const GI_T_MAX: f32 = 8192.0;
/// GI 射线没打到几何时注入的天空辐亮度系数（`gi_ray_radiance` 的 miss 分支）；
/// 1.0 = `sky_rgb()` 原样注入。这是 GI 的主要能量来源（天空经一次弹射照进阴影区）。
const GI_SKY_RADIANCE_SCALE: f32 = 1.0;
/// 命中自发光体素时的辐亮度增益（albedo · emissive · 本值）；1.0 = 不额外放大。
const GI_EMIT_GAIN: f32 = 1.0;
const GI_PI: f32 = 3.14159265;
/// RIS 权重的亮度投影（Rec.709）：抽样概率与 `w_sum` 都是标量，取权重的亮度投影做标量化。
const GI_LUMA: vec3<f32> = vec3<f32>(0.2126, 0.7152, 0.0722);

// ============================================================================
// 屏幕空间 ReSTIR GI（`gi/screen.wesl`，**GI 的唯一路径**）
//
// 业界形态（Ouyang 2021 / UE5 Hardware Lumen）：逐像素一个 reservoir，
// **新鲜候选 → 时域复用 → 空间复用 → 着色 → 降噪**。
// 本工程与业界的一处**结构性差别**（必须保持）：时域与空间两侧的「样本等价」判据都是
// **精确的几何面键**（体素 + 面 + 物体）＋「**着色法线**相似」，而不是业界常用的位置 / 法线启发式。
// 原因是本工程的硬约束（薄板 / 墙缝不漏光、逐体素平坦），位置容差在薄板上会漏光。
// 做法上不吃亏：同键 ⇒ 目标函数逐字相同 ⇒ 直并累计量是精确的 RIS 合并。
// **世界几何变过的那一帧不复用任何历史**（`gi/screen.wesl` 文件头 ⑥）：键只能证明"上帧那个面还在"，
// 证明不了"上帧累计进来的那些光路还成立"。
// ============================================================================

/// 每像素每帧的**新鲜候选**数（余弦加权半球射线，各含 ≤1 条太阳 NEE 阴影射线）。
/// GI 代价几乎全在这里：每像素的 trace 次数 = `本值`（GI 射线，命中几何时各再带 1 条太阳 NEE），
/// **空间复用一条射线都不发**（只并上一帧邻居的累计量）。
///
/// **只跟「降噪质量」档走，与分辨率档无关**（菜单「渲染/RESTIR GI」的两个 switch_group 正交）：
/// 「每屏幕面积的射线数」= 候选数 / 除数²，而**可见噪声 ≈ 每 texel 噪声 = σ_ray/√候选数**
/// （相邻 texel 的值已被时域累积与 atrous 强相关 ⇒ 上采样的双线性几乎不再降噪）：
///   · 1/1 + 4 → 4.0 条/屏幕像素，σ/2
///   · 1/2 + 4 → 1.0，σ/2
///   · 1/4 + 4（关/低/中 档）→ 0.25，σ/2
///   · **1/4 + 8（高 档）→ 0.5**（仍是 1/2 档的**一半**），**σ/2.83**
/// ⇒ 最高档把省下来的预算分一半给降噪，代价是**该档的 GI 射线翻倍**（全链最贵的一项）。
/// 注意最高档在 1/1 上也翻倍（8 条/全分辨率像素）——那是给「极限画质」用的对照点，不是推荐配置。
const GI_SS_CAND_N: u32 = 4u;
/// 每像素每帧的新鲜候选数 · **高 档（3）**（所有分辨率档共用）：射线翻倍。
const GI_SS_CAND_N_HQ: u32 = 8u;
/// 新鲜候选的**射线批大小**（ILP）：批内 B 条射线互相独立（只有方向不同）⇒ 先把它们全发出去、
/// 再按原本的 `k` 顺序做累计与蓄水池更新 ⇒ 方向、样本、比较顺序都不变、结果**逐位相同**，
/// 重叠的只是层次 DDA 的访存延迟。必须整除 `GI_SS_CAND_N` / `_HQ` 及二者的翻倍值（4/8/8/16），
/// 所以只能取 1/2/4；取 1 = 退化成逐条（用于降寄存器压力或 A/B 对照）。
const GI_SS_RAY_BATCH: u32 = 4u;
/// reservoir 的候选数上限 = 本系数 × **本帧新鲜候选数**（业界：M ≤ 20 × M_new）。
/// 超过就把 `w_sum` 与 M 同比例缩回（`w_sum / M` 不变 ⇒ 估计不变），只抑制历史对抽样的支配。
/// 以 `M_new` 而不是绝对值为基准是必须的：去遮挡帧的 `M_new` 翻倍 ⇒ 上限跟着翻倍，
/// 否则 boost 刚打进去的候选会立刻被上限压掉、boost 等于白做。
/// **记忆长度 = 本值帧**（窗内样本数 = 本值 × 每帧候选数，比值恒等于本值）：
/// 窗越长残余噪声越小（∝1/√窗）、但对**光照**变化（太阳/天光改写）的响应越钝 —— 32 帧 ≈ 0.53s@60fps。
/// **几何**变化不吃这个窗：世界一变整帧就不复用历史（`gi/screen.wesl` 文件头 ⑥），换了面/换了物体
/// 当场重建，颜色突变由 AABB 钳制兜住。
/// **本项零性能成本**（只换一个比较常量），同样与分辨率档无关。
const GI_SS_M_CAP_K: u32 = 20u;
/// 记忆窗上限（帧）· **高 档（3）**：32 帧（≈ 0.53s@60fps 的光照响应），残余噪声 ÷1.26。
const GI_SS_M_CAP_K_HQ: u32 = 32u;
/// 空间复用的候选 tap 数（上帧**本面所在位置**周围 3×3 环上的偏移个数）。
/// 每个通过判定的 tap 只**并累计量**（`w_sum` / `M`），**零射线**；两种 tap：
///  a) **同键**：主命中面键逐位相等 ⇒ 目标函数逐字相同 ⇒ 纯降噪、零偏差（面内平坦的主要来源）；
///  b) **同平面**：法线相似 + 平面检验通过（= 与降噪侧同一套判据）⇒ cosθ 相同、只有射线起点
///     差一格 ⇒ 抹平**相邻面之间的台阶**（这是「沿体素边缘的网格亮斑」的处方）；
///     1 体素薄板的另一侧过不了平面检验 ⇒ 不跨薄板、不漏光。
/// **绝不用邻居的方向重发射线**：那个候选既只在交界处触发（亮斑贴在体素边缘），
/// 又是按亮度加权抽出来的方向（源 pdf 不是余弦 ⇒ 权重 `π·L` 偏亮，且会被逐帧重新选中、在屏幕上
/// 漂移成「水下光斑」）。跨体素平滑一律走「并累计量」。
const GI_SS_REUSE_TAPS: u32 = 8u;
/// 空间复用的**权重上限**：借来的累计量总额 ≤ 本值 × 我们自己的 `M`（`1.0` ⇒ 最多各占一半）。
/// 为什么必须有：邻居的 `M` 上限与我们**相同**（`GI_SS_M_CAP_K` × 候选数），而 `GI_SS_REUSE_TAPS`
/// 个 tap 合计可达它的数倍 ⇒ 不设限时本帧那几条新鲜样本会被完全淹没，画面被"**上一帧 + 屏幕偏移
/// 一格**"的值支配（症状：相机一动就拖影，且暗块看起来钉在屏幕上）。
/// 缩放时 `w_sum` 与 `M` **同比例**缩 ⇒ 邻居的估计值（`sk_sum / sk_m`）不变，只压它的权重
/// ⇒ "抹平相邻面之间的台阶"这个原本的收益保留。`0` ⇒ 借来的证据权重为 0（等价于"只认同键"，
/// 但**不拒样本** ⇒ 不会触发 `hist_ok = false` 那条候选翻倍）。
const GI_SS_REUSE_M_K: f32 = 0.25;
/// 空间复用借来的**估计值**的钳制范围（× 本像素当帧自己的估计，即本帧新鲜候选的 `π·⟨L⟩`）。
/// **上限只压权重是不够的**：判据（面键 + 着色法线相似）证明的是"同一个**面**"，**不证明"同一个
/// 位置"** —— 面键允许沿平面任意平移（切向坐标不进判据）。相机靠近 / 远离时，`prev_pc` 附近
/// 那 8 个 tap 对应的世界位置随距离缩放而滑动，于是邻居**位置上的值**（及其长历史）被以
/// `GI_SS_REUSE_M_K` 的权重搬进本像素；在 GI 高对比处（背光面的暗部边界、遮挡边界）就表现为
/// 拖影（静止时 tap 对称 ⇒ 只是一次等权模糊、无方向性，所以**只有相机动才看得出来**）。
/// 钳到 `[LO, HI]` 倍之后，空间复用只能在本像素估计附近平滑 —— 借来的"位置差异"被切掉，
/// "台阶被抹平"的收益（相邻面差异通常在 2 倍内）保留。逐通道钳，与降噪侧 AABB 钳制同一手法。
const GI_SS_REUSE_CLAMP_LO: f32 = 0.5;
const GI_SS_REUSE_CLAMP_HI: f32 = 2.0;
/// **历史搜索**的候选 tap 数（0 = 关，8 = 重投影落点周围满 3×3；中心那一格恒为第一个候选）。
/// 时域复用的接受判据（`gi_same_surface`：面键 + 着色法线相似）**一个字都不改**，改的只是
/// 「去哪个像素找历史」：落点不通过时，在它的 3×3 邻域里按「十字优先、对角随后」再找几个。
///
/// 为什么需要它：一个体素面比一个 texel 还小的时候（斜面台阶、远处细几何），"包含重投影点的那个
/// texel"有相当概率把射线打到**相邻台阶**上 ⇒ 键不等 ⇒ 历史被丢掉、`M` 永远从 1 开始
/// （症状：静止干净、相机一动斜面就起雪花）。而本面在上一帧**确实有人采过**，就在落点旁边。
///
/// 代价：每多试一个候选多读 4 个字（flags / 键 2 / 法线），通过判据后才读累计量那 4 个字；
/// **没有任何射线**（判据只看键与法线；世界几何变过的那一帧整帧不复用历史 ⇒ 搜索也不跑）。
const GI_HIST_SEARCH_TAPS: u32 = 8u;

// ---- GI 降噪（`gi/denoise.wesl`：时域累积 + 迭代 atrous 两段）----
// 目标：把屏幕空间 GI 路的噪声压到静态无噪，同时**不引入 ramp、不跨薄板、不糊掉逐体素平味**。
/// 导引缓冲每像素字数：面键（2）+ **着色法线**（1，八面体 f16x2）+ 上帧重投影像素坐标（1）。
/// **不存世界位置 / 平面容差**：所有「同不同面」的判定 = 键（平面归属，精确位域比较）
/// + 着色法线相似（相似度，带容差 `GI_DEN_N_DOT`），两者都不需要位置。
const GI_DEN_GUIDE_WORDS: u32 = 4u;
/// 历史缓冲每像素字数：累积色 rgb（3）+ **输入**亮度**累积**一阶矩（1）+ **累积**二阶矩（1）
/// + 历史长度 M（1）+ 面键（2）+ **着色法线**（1）。两个矩按**与色完全相同的权重**累积
/// （`mix(hist, 本帧输入, 1−w)`），观测取的是本帧**实际输入** `c_in`（面内 / 同面平均 + 钳制后）
/// ⇒ `σ_hist = sqrt(M2 − M1²)` 是**输入的帧间波动**（NRD 的 m1/m2 就是这样量的），
/// 与 `GI_DEN_SIGMA_ALPHA` 直接可比（见 `gi/denoise.wesl`）。
/// 着色法线存进历史是为了**历史接受判据**（`gi_same_surface` 的相似度那一条）—— 导引是单缓冲、
/// 只有本帧，读不到上帧的法线。
const GI_DEN_HIST_WORDS: u32 = 9u;
/// 导引 word 布局下标。
const GI_DEN_G_KEY0: u32 = 0u;
const GI_DEN_G_KEY1: u32 = 1u;
const GI_DEN_G_NRM: u32 = 2u;
const GI_DEN_G_PREV: u32 = 3u; // x | y<<16；0xFFFFFFFF = 没有上帧对应像素
/// 导引「有效」= **键非零**（天空像素整格清零）—— **不要**另设一个有效位！
/// 曾经把有效位放在 `bit15`，而当时 `GI_DEN_G_KEY1` 里 **face 占 13..15 三个 bit**：
/// `face = 4 (-z)` 就是 `0b100 << 13 = 0x8000`，**与 bit15 撞车**；读侧再 `& 0xFFFF7FFF`
/// 把它抹掉 ⇒ `-z` 被读成 `-x`、`+z` 被读成 `+x` ⇒ 在体素凸角处（同一体素的 −x 面与 −z 面
/// 同时可见）「同面」判据误判成同一个面，把两个**互相垂直**的面混在一起（棱边渗色）。
/// 位域现在是 z 0..15 / face 16..18 / obj 19..31（见 `gi/common.wesl` 的 `gi_key1`），已占满。
/// 判据取「键非零」与旧世界空间缓存一致（`gi_entry_valid`），唯一的代价是「体素恰好在
/// (−32768,−32768,−32768)、face 0、物体 = 主世界」这样的像素会被当作无效 ——
/// `gi_key_ok` 把这个点排除在合法范围外，故撞不到。
/// 历史 word 布局下标。
const GI_DEN_H_COL: u32 = 0u; // 0..3 = 累积色 rgb
const GI_DEN_H_M1: u32 = 3u;
const GI_DEN_H_M2: u32 = 4u;
const GI_DEN_H_M: u32 = 5u;
const GI_DEN_H_KEY0: u32 = 6u;
const GI_DEN_H_KEY1: u32 = 7u;
const GI_DEN_H_NRM: u32 = 8u; // 着色法线（八面体 f16x2）—— 历史接受判据的相似度那一条要用
/// atrous 迭代次数（步长依次 1/2/4/8/16，足迹半径 = `GI_DEN_ATROUS_R` × 16 个 GI 像素）。
/// Rust 侧按本值决定派发几轮 / 每轮的 src→dst 组合（`DEN_ATROUS_ROUNDS`），范围 1..=5。
/// **取 5 = NRD/RELAX 的迭代数**：a-trous 的迭代次数就是它能覆盖的尺度数，3 轮（半径 7 像素）
/// 只吃掉近处大面上的噪声，落在「只占 1~2 个 GI 像素的小面 / 远处细几何」上的残留噪声
/// （同面键邻居太少 ⇒ 拉不平）要靠后两轮的大半径。每轮只是 GI 网格上的一次重采样，
/// 代价远低于 GI 射线本身；硬拒判据（不是同一个面就 cut）与半径无关，
/// 所以加大半径不会跨几何糊。
const GI_DEN_ATROUS_ITER: u32 = 5u;
/// atrous 单轮核的**半径**（以 tap 为单位）：核覆盖 `(2R+1)²-1` 个 tap。
/// **由 Rust 按菜单「渲染/RESTIR GI/降噪质量」的档位写进 `gi_den_cfg[0]`**（降噪 pass 够不到 `gi_u`，
/// 见 `bindings.wesl`），这里是两档的**权威值**（Rust 经 `wesl_consts.rs` 解析同一份源码）。
///   · `1` = 3×3 环（8 tap）—— **关闭/低 档（0..=1）**；
///   · `2` = 5×5（24 tap）—— **中/高 档（2..=3）**：同一足迹下采样更密 ⇒ 残余噪声按 √3 降；
///     代价是**足迹翻倍**（±2·step，5 轮后半径 32 个 GI texel = 1/4 档 128 屏幕像素）⇒ 画面更"平"。
/// 每轮多读的是导引（每 tap 3 个字）与一次纹理取值 —— 都只在 GI 网格上、没有射线，
/// 相对 GI 射线小得多（粗估 +0.1~0.3 ms 量级）。
const GI_DEN_ATROUS_R: u32 = 2u;
/// atrous 核半径 · **关闭/低 档（0..=1）**。
const GI_DEN_ATROUS_R_FAST: u32 = 1u;
/// **短历史稳定化**（NRD 的 `blurRadiusScale`；FidelityFX 的「低时域样本数 ⇒ 加大空间滤波」）：
/// atrous 每轮的有效步长按 `clamp(GI_DEN_STAB_FRAMES / max(M, 1), 1, GI_DEN_STAB_MAX)` 缩放。
/// 历史刚被丢掉的像素（`M = 1`）⇒ 步长 ×`GI_DEN_STAB_MAX`，**用更宽模糊代替生噪点**；
/// `M ≥ GI_DEN_STAB_FRAMES` ⇒ ×1（与稳定像素逐位相同）⇒ 只影响短历史的像素，稳态画面不动。
/// NRD 的原话就是这条取舍："If history gets discarded we see blur instead of noise"。
/// 代价：**零额外 tap**（只是同一批 tap 落得更远）。
const GI_DEN_STAB_FRAMES: f32 = 8.0;
/// 短历史时步长的最大放大倍率（`M = 1`）。
const GI_DEN_STAB_MAX: f32 = 3.0;
/// 时域统计的**邻域半径**（tap 为单位）：核覆盖 `(2R+1)²` 个 tap（含中心）。
/// 这两套统计（同面键的 σ、同面的 mean/std 与钳制范围）是 anti-ghosting 与本帧输入的全部依据，
/// 而它们**全都要求邻域非空**。取 `1`（3×3）时，斜面上「一个体素面 ≈ 一个 texel」⇒ `cnt` 常常
/// 停在 1 ⇒ ③′ 钳制整段不执行（历史色没有护栏 ⇒ 拖影）、③″ 也不触发（输入退回单 texel 原始噪声）。
/// FidelityFX 的阴影降噪器为此用 **17×17**（可分离两趟）取矩，`2`（5×5）是同一处方的最小可用档。
/// 代价：25 tap vs 9 tap（每 tap = 3 个导引字 + 1 次纹理取值，**没有射线**）。
const GI_DEN_NBR_R: u32 = 2u;
/// 时域累积的历史长度上限 M（帧）。M 封顶后历史权重上限 = (M−1)/M = 31/32 ≈ 0.969
/// ⇒ 稳态是「等权滑动平均 + 1/32 新信息」。
/// **取 32 = NRD / RTX Remix 的量级**（NRD 的稳态累积速度 ≈ 0.05 ⇒ τ ≈ 20 帧；Remix 暴露的
/// fast history 默认 6 帧），而 τ 直接就是「光照 / 远处几何变化」的响应时间：32 帧 ≈ 0.53s@60fps。
/// 本工程曾取 96（τ ≈ 1.6s）：静态残余闪烁更低（残余 σ ∝ 1/√M：M = 96 时 ≈ 9.3%、M = 32 时
/// ≈ 16.2%），但代价是**光照一变就拖** —— `σ_hist` 对**渐变**不敏感（每帧变化小 ⇒ `w_var` 一路是 1），
/// 只剩 (M−1)/M 这一项拦着。这个取舍见 README「不变量与踩坑」5i。
const GI_DEN_M_MAX: u32 = 32u;
/// 方差驱动的历史权重：`w_var = clamp(σ_α / σ_use, W_HIST_MIN, 1)`，
/// 其中 `σ_use = max(本帧同面键邻域（半径 `GI_DEN_NBR_R`）的亮度标准差, 累积矩给出的「输入帧间波动」σ_hist)`
/// （σ_hist 时间上平滑、σ_use 取 max ⇒ 既不逐帧抖，也对噪声突增保持灵敏）。
/// σ = 本帧邻域里**同面键** tap 的亮度标准差（同一个面内的差异**只可能是噪声**；
/// 换成「共面邻域」会把相邻体素的真实差异误当噪声，实测静态时间 σ 只降到 27%）。
/// **注意 σ 与 σ_α 是绝对值**（本工程 GI 的量级：gi_out ≈ ⟨L⟩，面内噪声 σ ≈ 0.005~0.01）：
/// 如果输入的噪声水平被抬高（例如某个分辨率档下面内平均失效），`σ_α/σ_in` 会塌向下限
/// ⇒ 历史权重掉到 0.05、**时域累积等于被关掉**（症状 = 雪花屏）。见 `gi/denoise.wesl` 的 ③″。
/// σ 大 ⇒ w_var 小（靠本帧，不拖影）；σ ≪ σ_α ⇒ w_var = 1（纯累积，静态收敛）。
const GI_DEN_SIGMA_ALPHA: f32 = 0.02;
/// 历史权重下限：压低但不清零（抗 flicker；真正的「历史作废」由面键逐位相等判定，不走这里）。
const GI_DEN_W_HIST_MIN: f32 = 0.05;
/// 颜色钳制系数（anti-ghosting 标配，作用于**同一个面**的邻域，半径 = `GI_DEN_NBR_R`）：
/// 历史色与本帧输入色都被钳到「本帧同面邻域均值 ± K × 标准差」。
/// 对**输入**的那一次是 NRD 的 pre-blur / outlier 抑制角色：GI 的原始估计是重尾分布
/// （偶尔一条射线打到强光面 ⇒ 离群亮点），不压掉的话它会在时域里被反复加权、在 atrous 里
/// 被摊成一片，表现为「偶发亮斑 + 闪烁」。用邻域统计量做一次钳制是最省的处方。
const GI_DEN_CLAMP_K: f32 = 2.5;
/// **着色法线相似度的容差**：`dot(n_a, n_b) < 本值` ⇒ 不算同一个面。
/// `voxel_normal` 给的是**邻域梯度法线**，取值是离散的一小撮：轴对齐（±e_i）、凸棱对角
/// （e_i±e_j)/√2、凸角体对角（±1,±1,±1)/√3。于是任意两法线的 dot 只可能是
/// 1 / 0.816 / 0.707 / 0.577 / 0.5 / 0.408 / 0 / …。
/// 取 **0.7**：同轴（1）与「朝同一轴倾斜 45°」（0.707，正是凸棱被平均出来的那个法线）都算同一个面；
/// 而「轴对齐 vs 45° 倾斜」之外的（0.577 的凸角、0 的垂直面）都拒掉。
/// 为什么需要这条：凸棱上的着色法线是斜的，光靠面键会把「看起来连续的那一段」切成噪点
/// （着色平滑、GI 却是碎的 ⇒ 棱边上一圈噪）。**留 0.9 就等于只认轴对齐**（下一档就是 0.816），
/// 那与「精确面键」几乎等价（也就丢掉了这条判据的意义）。
/// 容差再放宽到 0.55 会把凸角法线（0.577）也并进来（更平、但会把着色差异更大的两个面混起来）。
const GI_DEN_N_DOT: f32 = 0.7;
/// 亮度 range 权重：`exp(-|Δluma| / φ)`，φ = `GI_DEN_LUMA_K` × max(σ_n, 本值 × 参考亮度)。
/// 这条只作用于**同一个面**的 tap 里「不是完全同键」的那些（同面键满权重，见 `gi/denoise.wesl`）。
/// 取 1.0（= φ 就是面内噪声 σ）：同一平面上相邻体素的典型亮度差（实测平面 σ ≈ 0.016，相邻体素
/// 差 ~0.01）会得到 w ≈ exp(-0.01/0.0045) ≈ 0.1 ⇒ 跨体素注入几乎关掉。取 3.0 时 w ≈ 0.48，
/// atrous 会把邻居体素的值混进面内 ⇒ 面内被「糊出梯度」（实测面内空间 σ 卡在 28~30% 下不去）。
/// 这直接对应「逐体素平坦、不允许把体素面糊成渐变」这条硬约束。
const GI_DEN_LUMA_K: f32 = 1.0;
/// φ 的**相对下限系数**：φ 至少是本值 × 参考亮度（参考亮度 = 共面邻域均值的亮度）。
/// 它只是**最后一道地板** —— 主尺度是实测的噪声量（同面键 σ / 累积矩；同面键邻域只剩自己时取
/// 共面邻域的实测离散度，见 `gi/denoise.wesl` 的 φ 段）。
/// **没有这道地板会是个 bug**：那时 φ 可能 ≈ 0 ⇒ 跨面 tap 的亮度权重全塌成 0 ⇒ 大半径那一轮
/// 完全空转，只剩它自己的噪声。现象：相机静止时干净（时域累积就够了）、一运动远处大面就起
/// 「细密高频雪花」。
/// 取 0.05（5%）：平面内 GI 是低频的 ⇒ 拿到 w ≈ 0.5~1（真正起作用）；而真实边界早被
/// **法线 + 平面检验**硬拒了，不靠这条亮度权重挡。调大 ⇒ 更平更糊（跨面注入更强）；
/// 调小 ⇒ 保留更多细节、雪花回潮。
const GI_DEN_LUMA_FLOOR_REL: f32 = 0.05;

/// 天光环境项系数：**常量天光**按 `sky · 本值 · mix(1, FLOOR, cov)` 加在着色点上。
/// `cov = 0`（GI 无数据）时它就是巨量的兜底；顺便也是二次顶点唯一的天空项入口（见 `gi/ray.wesl`）。
const GI_SKY_AMBIENT: f32 = 0.05;
/// 天光常量的地板：`cov = 1`（GI 有数据）时保留 k 倍这一项。
/// 必须写成 `mix(1.0, GI_AMBIENT_FLOOR, cov)`（语义 `1 - (1-k)·cov`）。
const GI_AMBIENT_FLOOR: f32 = 0.01;
/// 着色点从体素表面再外推的量（体素）。
/// 必须与 Rust `brickmap::dda::wgsl_consts::SHADOW_SURFACE_EPS` 一致。
const SHADOW_SURFACE_EPS: f32 = 0.03125;