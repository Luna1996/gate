So, before anyone starts typing any heated comments, I admit it. The title is clickbait. For those who don't know, Rayon is a popular multi-threading library for Rust, and it's arguably quite useful. The library is mature, meticulously tested, and easy to set up. But, the design choices behind Rayon don't align well with the real-time needs of video games. So, in this video, I want to talk about those limitations and how and why I built my own custom lock-free thread pool for my voxel game engine. CPUs nowadays come with many cores. This means that your computer can run multiple pieces of code or threads at the exact same time. Rayon makes it easy to distribute work across those threads. For example, this program iterates over a bunch of numbers and calculates their square roots. By changing the iterator to one of Rayon's parallel iterators, the loop body will run on multiple threads, making the whole program faster. This style of multi-threading is super simple and expressive. There's no need to schedule callbacks or worry about synchronization barriers or anything like that. You just write code that looks synchronous, but with parallel loops. Rayon also allows you to spawn background tasks like this, which do run asynchronously. [music] Now, in my game engine, there are many places where this is handy. World generation, calculating acceleration structures, physics, [music] and so forth can all be parallelized. Initially, I was using Rayon for this, until I noticed the lag spikes. What was happening was that the main thread would need some parallel work done immediately, like the physics, and all of the background threads would be busy with world generation, and this would cause a stall. And so this is the systemic issue with Rayon that I do want to address, which is that it's designed for [music] throughput, not latency. Throughput is the average amount of data your code processes over time. The higher your program's throughput, the faster it is. Latency, on the other hand, is how quickly [music] your code can get to an answer for a particular call. So if the main thread schedules 5 milliseconds of physics work, but then waits 20 milliseconds while Rayon is busy with other stuff, that's 20 milliseconds of added latency on the main thread. Let me be a bit more specific about how this works. At startup, Rayon creates a fixed thread pool of workers. When an external thread, like the main thread, wants to run a parallel iterator, it puts that iterator into one of the workers' queues. When the worker thread is ready, it will begin processing that iterator. And if other worker threads are free, they will work steal and grab parts of that same iterator to process themselves. Once the whole thing is done, control returns to the main thread. Asynchronous background tasks are also added to the worker queues in the same way. The biggest issue here is that the external thread doesn't participate in the work. If the workers are busy, there's no bound, time-wise, on how long the main thread waits for its work to begin. It's also extremely likely that a context switch occurs whenever the main thread schedules work. The OS will swap the main thread out for something else, which is wasteful. For a command-line application where you're just churning through data, that doesn't matter. All of your threads are busy, so you're getting maximum throughput anyway. For games, though, it can be the difference between a smooth and stuttery experience. To make this more concrete, here's my physics engine simulating 1,000 boxes. With Rayon, once the boxes settle, uh the physics would take 12 milliseconds on average per tick. And here's the same thing in a profiling tool. So, on the horizontal axis here, we have time. And then, on axis we have each thread. So, these colored boxes indicate what the threads were doing over time. And so, [music] the main thread, which is the top one here, starts out by beginning the physics step. It does some initialization &gt;&gt; [music] &gt;&gt; and sorts all of the objects into islands. And because most of the boxes are touching in this example scene, most of them end up in this one big island. Anyway, after the main thread is done with its early work, it dispatches all of the islands to the workers, so the workers can simulate them in parallel. [music] And so, we can see that over the course of this particular tick, all five of my worker threads picked up and ran a bunch of small islands. And worker number two got stuck with this really long task to simulate the big island. Um this So, this isn't bad, 12 milliseconds per tick. But, one thing is immediately obvious, which is, as I was saying before, the main thread is just sitting [music] idle this entire time. It's waiting for the islands that it sent to the workers to finish running. Now, you might argue that one way to sidestep this is to just add another worker to the thread pool, so that it can run instead of the main thread. But, the problem with that is the the main thread might want to go off and do other things while the workers are running. So, if you add any more threads here, you'll end up um potentially oversaturating the CPU because all [music] of the workers and the main thread try to run at the same time, and that will lead to lag. The CPU won't be able to run all of the threads at once. One other thing to note is that sometimes workers will pick up completely unrelated tasks while they're processing. So, for example, in this particular uh island task [music] that worker number three had, it began by running collision detection, and then it had some parallel work to do. [music] It wanted to do graph coloring and um mask calculations at the same time. And so, it began with the graph coloring, and then engine number one jumped in to help with the mask calculations. And so, engine worker number three was left waiting for those mask calculations to finish. And so, it began grabbing just other tasks and running them. Now, this I know I've been talking a lot about not letting threads sit idle. So, this is good except for the fact that Rayon doesn't distinguish between which task gets picked up when. So, instead of these being simulate island tasks, they could have been like a world generation task that would take 50 milliseconds to complete, and then you'd have a stall. All right. So, that was the problem. But, what did I do about it? What do you think we do on this channel? I rewrote Rayon from scratch, of course. My library is called micropool. Check it out. It's got the join primitive. It's got background tasks, and it even supports parallel iterators using the paralite library. But, what makes this different? First off, whenever a thread runs a parallel iterator, that thread actively helps to complete the work, even if it's an external thread. So, this means that the main thread doesn't sit idle. And beyond that, the library remembers which external thread originally spawned a given task and any subtasks or sub iterators. If a worker is waiting for the results of a parallel iterator, it might grab some other work, but it will only ever grab work that was spawned from the same original root task. So, this prevents time-sensitive stuff, like physics, from getting stalled because a thread decides to do world gen while it waits. The coolest part about this, though, is that the scheduler is lock-free. Pretty much everything is done with atomics. Multiple threads can spawn new tasks and look for available work simultaneously. And this wasn't how I started out. Initially, I had a much more traditional job system, which had a tree of jobs behind a read-write lock. But, when I looked at the results in the profiler, it looked like this. See all of these orange sections? That's very bad. They indicate contention. Threads waiting for the lock so that they don't interfere. And this problem just gets worse and worse with the more threads you use. So, this was what made me say, "I got to go lock-free." So, to round things off, I thought we'd do a tiny little code walk and see how this actually works in micropool. When a thread goes to schedule a parallel iterator, that parallel iterator becomes a job. And each job has one or more work units. For an iterator, a work unit could be like one element in the iterator, or more likely it'll be a group of elements that the background threads process together. So, each work unit is processed by exactly one thread. The information for each job is stored in this job structure, and this is really the heart of micro pool. The job structure has an atomic counter, which tracks the number of work units that haven't been started for the job, and then it has a pointer to additional information. Um and this additional information includes a function to call to actually run work units, and a tracker that remembers how many work units have not finished yet. So, when a parallel iterator is scheduled, it'll fill out one of these job slots, and micro pool maintains a fixed array of these slots. Um so, it'll fill out a job slot, and then as background threads are going around looking for work to do, they'll just go through the array, and they will decrement uh they'll subtract one from this available units counter, and if that fetch sub atomic operation returns a positive value, then the worker thread knows it has reserved a work unit. Uh whereas if this returns a negative value, it indicates that all of the jobs have all of the units for the job have already been taken, or there was no valid job at all. If a worker thread has successfully reserved a job, it will then go ahead and call this um function to run work units. And once all of the available work units are exhausted, it will subtract the number of work units it completed from this incomplete units counter, which tracks how many units have yet to finish. Once this counter hits zero, uh that's how the main thread knows that the parallel iterator is completely done, and it can move on. The only other uh thing to make note of here is that there's some bookkeeping involving which job slots are taken and which job slots are available, and also tracking the root task um that's responsible for any job. So, all of that is done with this atomic bit structure. &gt;&gt; And what this is is it's just a big array of booleans that have been bit packed into integers. And so, this structure allows for um setting elements as well as efficiently um iterating through the ones and the zeros. And this is possible because you can read a single 64-bit integer and then look at the 64 booleans contained within it. When searching for work [music] to complete, worker threads will look at an atomic bit mask, and they'll only choose jobs that are marked within that bit [music] mask. And so, this helps separate time-critical stuff, like the physics, from just background tasks, like the world generation. There's really not much more to the architecture here. The real challenge with this library is dealing [music] with all of the details of atomic orderings and the lifetime invariance, and making sure that threads go to sleep and wake up at the appropriate times. Nonetheless, the library is unit tested, and I've run everything through [music] the Miri sanitizer without any problems, so it works great. Here we are back in Tracy, but this time with micro pool. The average time it takes for that boxing from before um is now down to 8 milliseconds over the original 12 milliseconds with Rayon. So, I was really ecstatic when I saw this uh because just by swapping out the thread pool library, I was able to make my program much faster. Like Rayon was 50% slower &gt;&gt; um than micro pool in this case. And the first thing to notice is that the main thread is participating in helping to complete work. It's grabbing jobs from some of the background [music] threads, especially this big green thread. It's helping out with a lot of the collision detection and solving. And all of these smaller jobs are running independently. [music] None of them are picking up world generation tasks or other things that might cause lag. And now my voxel engine runs buttery smooth. That brings us to the end of the video. I know this was more technical than usual, so please leave a comment down below letting me know what you think. Otherwise, don't forget to like and subscribe and have a lovely day.