# R2 基础渲染

> 施工优先级：同类内保持原编号顺序；新编号为文件内全局顺序号，旧 `Px.y` 编号保留作历史锚点。

- [ ] **R2-1（3.3）⚠️ 2026-09-04 后置（电路玩法管线前提，见 README「当前主线重定向」）** **状态调制通道**：StateTable buffer + shader 查表（先用占位数据做呼吸/闪烁验证，P6 接真数据）
- [ ] **R2-2（3.5e）** **方向位掩码 LUT 预过滤（对标 #18「提速一倍」核心项，1660 降档池候选）**：编译期生成查找表 `(入口位置 × 主方向组合) → 该射线可能命中的体素位掩码`——**与数据无关的纯几何先验**（#18 字幕确认），运行时 occupancy mask AND 方向 mask = 0 → 整块跳过（几条 GPU 指令）；直接作用于 brick tree 各层 u64 mask（v4 换轨后每层天然存在，无额外构建成本）；**LUT 规模关键结论**：只需覆盖 4³ 中块粒度，入口 16²×6 面 × 8 方向桶 = 12288 项 × 8B = **96KB 常量 buffer**（若按 fine 粒度 4096-bit mask 需 6MB，不划算）；步进子块前一条 AND 即可整块跳过；**降档触发点**：3.7 性能验收若 1660 不达标则启用
- [ ] **~R2-3（3.5f）~** ~~DDA 访存压缩三件套~~ — **已被 v4 Brick Tree 换轨吸收，废弃**。①细步寻址链缓存：五步寻址链（TileIndex→Bitmaps→Dirs→Node→Slot）已删除，现为 tree_base + DFS，无链可缓存；②per-cell 64-bit L2 occupancy mask：brick tree 节点本身就是 u64 mask，层次栈式 mask DDA 已实现「mask 进寄存器后子块步进零 load」；③tile 级第三级 DDA：chunk 窗口 A&W 已实现（空 chunk 1 load 跨越）。原条目针对旧 TileGrid 五步链写成，全部失效
- [ ] **R2-4（3.5g）** **Beam 两级渲染 + 阴影降档（对标 #18，1660 降档池首选）**：①**Beam**——低分辨率 pass 完整跑 trace+shade 额外输出 R32F 命中距离 t，全分辨率 pass 以周围 4（斜向布局 6）条低分辨率射线的 **min(t) 为射线起点**（平均空域行程大幅缩短），低分辨率覆盖深度之外从 0 正常步进；**保守性证明**（#18）：低分辨率 pass 限制步进深度，保证「任何大到足以覆盖某全分辨率像素的体素」必被某条低分辨率射线命中 → 输出与非 beam 版**逐像素一致**；**OBJ 物体必须在低分辨率 pass 内参与**（一致性前提）；当前 RenderScale 全分辨率 1:1 无降采样，beam 优于纯半分辨率（着色保持全分辨率且可证明无误差）；②**阴影降档**（beam 思想变体）——阴影是低频信号：半分辨率阴影可见性 + 全分辨率主射线（**软阴影锥采样已废弃**——Douglas 最终方案仅 per-pixel 1 条硬阴影射线，见 docs/douglas-final.md §5.1）；**降档触发点**：3.7 验收 1660 不达标启用
