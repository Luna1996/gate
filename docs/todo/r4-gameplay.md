# R4 玩法外壳

> 施工优先级：同类内保持原编号顺序；新编号为文件内全局顺序号，旧 `Px.y` 编号保留作历史锚点。

## P4 交互桥接（渲染 → 游戏逻辑的转轨点）

- [ ] **R4-1（4.1）** CPU 拾取：体素 DDA（与 GPU 遍历同构，支持剖面），返回命中体素（含层级）+ 进入面朝向（**动工前先裁决：编辑刷子粒度切换机制，见 README UNRESOLVED 表**）
- [ ] **R4-2（4.2）** **编辑闭环**：Bevy 输入 → batch_edit → 脏上传 → 画面同帧更新
- [ ] **R4-3（4.3）** 视觉反馈：悬停高亮 / 幽灵预览 / 元件轮廓
- [ ] **R4-4（4.4）** 基础工具：放置 / 删除 / 涂材质 / 直线刷
- [ ] **R4-5（4.5）** **剖面切割**：DDA 起点沿裁剪平面推进（任意角度），编辑与观察封闭电路内部（M2 管线预留位在此兑现）
- [ ] **R4-6（4.6）** 验收：连续高速编辑无卡顿、无渲染不同步；跨级编辑（粗→细）无明显卡顿尖峰（**资源断言 v3.1**：单次编辑 CPU 耗时上界 + 编辑会话内存增量上限，脏上传连发不积压）
- [ ] **R4-7（4.7）** **OBJ-2 平滑移动与交互**：物体 = Bevy Entity（`Transform` 驱动渲染 descriptor，每帧上传，体素网格本身不动 → 平滑无对齐伪影）；抓取 / 拖放 / 放置输入桥（复用 P4 输入体系与 R4-1 拾取——拾取射线同样走 `trace_scene()`）；物体间 / 物体-世界遮挡拾取排序正确

## P6 游戏逻辑 II：事件驱动电路模拟（gate-voxel）

- [ ] **R4-8（6.1）** 元件行为语义表（材质 → 类型 → 真值函数，数据驱动可扩展）
  - [ ] 首批：导线 / 与 / 或 / 非 / 时钟 / 输入端口 / 输出端口
- [ ] **R4-9（6.2）** 事件驱动核心：双缓冲脏元件队列，门延迟 ≥1 tick，成本∝翻转数
- [ ] **R4-10（6.3）** 固定步长模拟时钟：tick 率可配（60~10kHz），与帧率解耦
- [ ] **R4-11（6.4）** StateTable 更新 + 每 tick 上传（真数据接 R2-1 通道，游戏"通电"）
- [ ] **R4-12（6.5）** 每 tick 工作预算（~2ms）+ 过载降速 + 状态暴露给 UI
- [ ] **R4-13（6.6）** 调试接口：暂停 / 单步 / N 步 / 元件电平查询 / 逻辑分析仪数据源
- [ ] **R4-14（6.7）** 确定性回放：输入激励哈希（排行榜验证地基）
- [ ] **R4-15（6.8）** 差量存档：非空 tile + palette + 组件表 + 侧表 + 状态表，版本头兼容
- [ ] **R4-16（6.9）** 压测：满屋环形振荡器（预算降速）；8 位脉动 CPU 基准（兼营销素材）（**资源断言 v3.1**：tick 处理时间分布 ≤ 预算断言、事件队列 + StateTable 内存上限；过载降速路径本身要有测试覆盖）
- [ ] **R4-17（6.10）** **线程池纪律（对标 #27「Rayon is NOT for games」）**：tick 预算任务（紧急）与后台任务（远处体素上传/烘焙/程序化生成）**根任务掩码隔离**——worker 不跨根任务窃取，主线程亲自参与并行迭代（不 idle 干等）；gate 当前 P2.2 用 rayon 跑砖块图构建，P6 模拟 tick 并行时必须遵守此纪律，否则后台 50ms 体素烘焙可能顶掉紧急 tick 任务（#27 Tracy 实测 Rayon 三宗罪：主线程 idle / 上下文切换 / 工作窃取不分任务来源）；实现参考 #27 micropool（无锁 + 固定 job slot + 原子计数器 + 位压缩槽位掩码）或给 rayon 套同款纪律

## P7 完整编辑 UX（Zach-like 手感主战场）

- [ ] **R4-18（7.1）** 批量操作：框选 / 吸附 / 镜像 / 复制粘贴 / 旋转
- [ ] **R4-19（7.2）** 撤销重做：命令栈（与存档格式同构）
- [ ] **R4-20（7.3）** 观测工具 UI：元件信息卡 / 电平探针 / 逻辑分析仪（统一 UI 框架 + Modern UI 风格，见 README 决策表）
- [ ] **R4-21（7.4）** 相机打磨：阻尼、快捷键（手柄映射随 P12 后置）
- [ ] **R4-22（7.5）** 音频占位：UI 音效 / 探针哔声 / 通电反馈音（音乐后置）
- [ ] **R4-23（7.6）** 手感迭代：自测清单驱动（真人试玩渠道未建，启用时恢复采集流程）

## P8 谜题框架与关卡

- [ ] **R4-24（8.1）** 关卡格式：允许区域体素掩码（任意形状）+ 预置电路 + 目标定义
- [ ] **R4-25（8.2）** 编辑校验层（谜题模式掩码校验，沙盒直通）
- [ ] **R4-26（8.3）** 目标判定：输入激励序列 → 期望输出比对
- [ ] **R4-27（8.4）** 评分：拍数 / 元件数 / 占用面积
- [ ] **R4-28（8.5）** 解法字符串编解码（本地分享，不依赖工坊）
- [ ] **R4-29（8.6）** 谜题内容：教学 3 + 基础 6 + 进阶 6（自测迭代，真人测试渠道启用后升级）
- [ ] **R4-30（8.7）** 沙盒模式：无校验无限扩张 + 过载指示器

## P10 工作间场景与资产管线

- [ ] **R4-31（10.1）** .vox 导入器：MagicaVoxel → palette 映射 → 锁定体素注入（跳过洪泛）；**双目标输出：世界网格注入 + OBJ 物体网格装载**
- [ ] **R4-32（10.2）** 工作间雕刻 + 打光（光源为点光源列表，落进「暗色实验室」主题配置）
- [ ] **R4-33（10.3）** 元件预制件库：芯片 / 端口 / 显示元件
- [ ] **R4-34（10.4）** 背景与工作区隔离：背景 tile 不进洪泛/编辑，一次上传永驻
- [ ] **R4-35（10.5）** **OBJ-3 物体编辑 = 修复玩法**：NPC 寄来的电子产品 = 预制件装载为 OBJ 物体（R4-31 双目标 + R4-33 预制件合流）；修复 = 对**物体局部网格**的逐体素编辑（复用 R4-2 编辑闭环与脏上传管线，编辑目标切物体槽位）；**动工前先裁决：修复判定语义（图案比对 vs 物体网格进洪泛/模拟），见 README UNRESOLVED 表**
