# R5 其他

> 施工优先级：同类内保持原编号顺序；新编号为文件内全局顺序号，旧 `Px.y` 编号保留作历史锚点。

- [ ] **R5-1（3.8）** **达成即理论可上架**：Steam 页截图素材先备份

## P11 渲染 M4：冲刺线（可无限期推迟）

- [ ] **R5-2（11.1）** ReSTIR 精修直光（M3 降噪稳定后再动）
- [ ] **R5-3（11.2）** 玻璃透射（折射射线）；**半透明路线结论（#16 对照）**：gate 全 ray marching **不做 OIT**——OIT 加权平均是光栅化遗产（无序 fragment 才需要排序消除），ray marching 射线天然 front-to-back 有序；水体/玻璃走 **DDA 穿透累积**：命中半透明体素时按不透明度累积 transmittance + 颜色后继续步进（改 `trace_world` 命中分支「不透明度 <1 → 累积+继续」，palette 预留透明通道即可），折射射线后置
- [ ] **R5-4（11.3）** 逐体素动画：电流流动（comp_id → 动画参数查表）
- [ ] **R5-5（11.6）** **OBJ-4 烘焙/解烘焙优化（可无限期推迟）**：静止物体烘焙进世界网格（省逐帧多网格遍历 + 纳入 GI/洪泛），拿起时反向解烘焙
- [ ] **R5-6（11.7）** **体素物理避坑清单（对标 #11/#20/#26/#28——gate 自研物理时的施工图与避坑参考）**：
  - [ ] **碰撞检测旋转不变性（#26）**：体素分类 corner/edge/face/interior，只测 edge-edge 与 corner vs (corner/face/edge)；**折中方案 = 角/棱取球面、面保持全平面**（球-球测试 + 少量平面-球 + 棱对棱圆柱特例，数学量与球法相当，伪影消除——v2 纯球法可见下陷+多余摩擦、纯 AABB 法线旋转不变性失败抖动）
  - [ ] **求解器 TGS + warm starting（#26）**：split impulses 因浮点精度让静止盒子 jitter（先积分再压速度再分开位置）→ 改 TGS（加力→解速度约束→更新位置内循环多次迭代，复用已有接触不重跑碰撞检测）+ **warm starting**（上一帧接触力为初值）；体素场景天然优势 = **用「两碰撞体素坐标对」作 hashmap key 关联帧间接触**（常规引擎要造唯一 contact ID）
  - [ ] **SAT 线性变换复用（#11）**：体素全网格对齐 → 单位体素预计算投影区间 + 每对体素碰撞检测仅 1-2 次矩阵乘（区间线性变换）；八叉树空区整块免检
  - [ ] **破碎/断连（#28/#12）**：硬碰撞分裂物体，对标 Teardown；检测裁剪与物理复用同一套 corner/edge/face/interior 标注；连通分量标记做结构破坏（与 R1-1 flood-fill 同算法，#12 SVO 同质八分体加速可复用）
  - [ ] **逐体素材质属性（#26）**：密度/摩擦/弹性（钢比羽毛重，为流体沉浮铺垫）；与 gate Palette 的 roughness/emissive 字段对齐扩展
  - [ ] **2D 玩具引擎先行验证数值算法**（#20/#26 方法论）：任何数值模拟特性先 2D 化验证再 3D 落地
  - [ ] **四元数教训（#12）**：任何不可交换/非对易的数学结构（四元数、矩阵、变换序）在代码里显式标注乘法顺序（#12「幽灵自转」三周调试根因 = 四元数乘法两边写反）

## P12 平台与发布（**整体后置**：Steam 页面暂不开架；仅 P0 的 Platform trait 抽象保留。P10 完成且质量达标后再评估本阶段）

- [ ] **R5-7（12.1）** steamworks-rs 薄集成 + Platform trait（Null 后端保证裸跑/CI）
- [ ] **R5-8（12.2）** steamcmd + VDF 脚本化构建上传
- [ ] **R5-9（12.3）** Steam Cloud（存档格式落地时同步接入，勿后补）
- [ ] **R5-10（12.4）** Steam Input 手柄映射（键鼠首发已定，手柄在此阶段随发行需求评估）
- [ ] **R5-11（12.5）** 设置系统：画质档（M2/M3 可切）+ 模拟 tick 率 + 音量
- [ ] **R5-12（12.6）** 本地化框架（字符串表，中英先行）
- [ ] **R5-13（12.7）** 冲刺期：商店页六图 + 胶囊图 + 90 秒预告（暗色实验室 M3 画面）

## P13 测试与质量

- [ ] **R5-14（13.1）** 确定性测试 CI 化（回放哈希防回归）
- [ ] **R5-15（13.2）** 性能基准场景库：标准 CPU 电路 / 满载工作台 / 巨型沙盒 / 振荡器地狱 / 混合分辨率极端场景
  - [ ] 每场景绑定**资源上限断言**（v3.1）：系统内存 / VRAM / 每帧 CPU 耗时 / 每 tick 预算——CI 跑宽松上界（防机器抖动误报），基准机（1660/3070）跑正式数字报告
  - [ ] 内存回归追踪：峰值 RSS / VRAM / GPU buffer 用量记进 CI 日志，趋势逼近预算即预警
- [ ] **R5-16（13.3）** 跨 GPU 验证：NVIDIA / AMD / Intel（DX12/Vulkan 后端均测）
- [ ] **R5-17（13.4）** 存档版本迁移测试
- [ ] **R5-18（13.5）** panic 隔离策略：模拟/渲染崩溃不闪退
