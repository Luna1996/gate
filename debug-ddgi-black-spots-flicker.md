# Debug Session: ddgi-black-spots-flicker

## Status
[OPEN]

## 问题定义
- **症状（Actual）**：DDGI 开启后画面存在黑斑（局部暗块/黑块）与闪烁（时间性亮暗抖动/跳变）。
- **期望（Expected）**：与 Douglas #23 视频一致——黑斑在数秒~数十秒内收敛消退，稳态后无可见闪烁。
- **影响范围**：DDGI 间接光输出（trace 后 blit 前）；直光/阴影不受影响。

## 已知背景（已排除的根因，本轮不重复怀疑）
多轮会话已修复并验证：级联 slot 身份（shifted copy 搅乱）、样本缓冲越界发散、
next→prev 镜像回拷缺失、每探针射线数下限 16、滚动增量烘焙。
探针网格已定版：base 16³（= Douglas LOD0）+ 4 滚动级联 32/64/128/256（LOD1-4 档）。

## 复现步骤
1. `cargo run --release`（F5），Debug View 打开 DDGI GI。
2. 静止相机 60s：观察黑斑是否收敛、稳态是否闪烁。
3. 缓慢环绕相机：观察闪烁是否加剧、黑斑是否跟随滚动边带。

## 假设表（Step 2）
| ID | 假设 | 可能性 | 成本 | 预期信号 |
|----|------|--------|------|----------|
| A | 深度可见性权重把邻居探针全剔（w≈0）→ 黑斑；权重随帧抖 → 闪烁 | 高 | 低 | Debug View wsum 模式：黑斑处权重≈0（黑）；GI 模式同处不黑（数据本身有值） |
| B | 黑探针数据问题：age≥1 但 irr 全零/暗（blk 探针）被当有效采样 | 高 | 低 | readback census：blk>0 持续；blk 的 age 分布集中在新值（滚动重置）或高值（从未更新） |
| C | 选域闪烁：ddgi_dom_contains 在级联边界像素级翻转 → 亮度跳变 | 中 | 低 | Debug View Domain 模式静态相机：边界颜色逐帧翻转 |
| D | 滚动帧扰动：shifted copy/新列复位在滚动帧产生瞬时黑带，相机滚动越频繁越明显 | 中 | 中 | 日志 scroll 行与闪烁时刻相关；静态相机无滚动则症状消失 |
| E | 收敛/预算问题：每帧射线预算被钳制（clamp>0）或活跃集翻转（age 长期 0/255），EMA 方差持续高 | 中 | 中 | readback：c*:clamp 持续 >0；a0 占比高且随帧大幅波动 |

## 证据记录（Step 7 填写）
（待复现运行后填写）

## 运行记录
- runId=pre-fix：待运行
