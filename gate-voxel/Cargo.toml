[package]
name = "gate-voxel"
edition.workspace = true
version.workspace = true
license.workspace = true

[dependencies]
# 纯逻辑，零渲染依赖
glam = { workspace = true }
# chunk 级并行（compact_all / 批量导入）
rayon = { workspace = true }
