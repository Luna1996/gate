{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build-gate-app",
      "type": "shell",
      "command": "cargo",
      "args": ["build", "-p", "gate-app"],
      "options": { "cwd": "${workspaceFolder}" },
      "group": { "kind": "build", "isDefault": true },
      "problemMatcher": ["$rustc"]
    }
  ]
}